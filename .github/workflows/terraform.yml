name: Terraform Destroy On Request
on:
  push:
    branches:
      - destroy     #### branch trigger việc destroy
env:
  AWS_REGION: us-east-1

jobs:
  destroy_terraform:
    name: Destroy Terraform Infrastructure
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./terraform

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v2

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Terraform init
        run: terraform init

      - name: Destroy EKS first
        run: terraform destroy -target=module.eks -auto-approve

      - name: Sleep for ENI cleanup
        run: sleep 180   # 3 minutes (AWS cần thời gian delete ENI)

      - name: Terraform destroy
        run: terraform destroy -auto-approve

      - name: Confirm destroy completed
        run: echo "🔥 Terraform destroy completed. All resources removed."

        